#include "intake.hpp"

void asyncIntakeControl(void *param) {
  // Minimal placeholder intake task.
  // Expand with actual intake motor control logic.
  (void)param;
  while (true) {
    pros::delay(20);
  }
}
